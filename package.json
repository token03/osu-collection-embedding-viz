{
  "name": "visualizer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run server.ts",
    "build": "bun build ./src/index.ts --outdir=dist --minify && cp index.html dist/ && cp style.css dist/ && cp -r viz_data dist/",
    "preview": "cd dist && python3 -m http.server 8000"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/d3": "^7.4.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev18.0",
    "d3": "^7.9.0",
    "lit-html": "^3.3.2",
    "regl-scatterplot": "^1.15.0"
  }
}
